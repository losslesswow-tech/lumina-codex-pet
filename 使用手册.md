# Lumina 中文使用手册

Lumina 是一只白发、青绿色眼睛的动漫龙娘，也是适用于 ChatGPT/Codex 桌面应用的自定义动画宠物。

## 文件说明

- `spritesheet.webp`：宠物动画图集，尺寸为 1536 × 2288，使用 v2 布局。
- `pet.json`：宠物名称、描述、版本及图集文件配置。
- `contact-sheet.png`：常规状态动画预览。
- `look-directions.png`：16 个注视方向的预览。
- `validation.json`：图集结构验证结果。
- `run-summary.json`：本次宠物制作与质检摘要。

## Windows 安装

1. 下载仓库中的 `spritesheet.webp` 和 `pet.json`。
2. 打开文件资源管理器，在地址栏输入：

   ```text
   %USERPROFILE%\.codex\pets\lumina
   ```

3. 如果 `lumina` 文件夹不存在，请新建它。
4. 将 `spritesheet.webp` 和 `pet.json` 放入该文件夹；更新旧版本时选择覆盖同名文件。
5. 打开 ChatGPT/Codex 桌面应用，进入 **设置 → 宠物**。
6. 选择 **刷新**，然后选择 **Lumina**。

也可以在支持宠物的交互式 Codex CLI 会话中输入：

```text
/pets lumina
```

## 显示或隐藏宠物

- 在桌面应用中输入 `/pet`，或从命令菜单选择“显示宠物”。
- 右键宠物并选择“隐藏”，可以关闭悬浮宠物。
- Windows 默认快捷键为 `Windows + Alt + P`，可唤出宠物控件并聚焦快速聊天。

## 动画状态

Lumina 会根据任务状态自动切换动画：

- `idle`：空闲待机。
- `running`：对话或任务正在执行。
- `waiting`：等待你的输入、批准或决定。
- `review`：专注检查或审阅。
- `failed`：任务失败或遇到系统错误。
- `running-left` / `running-right`：拖动宠物时的左右移动动画。
- `waving`：挥手互动。
- `jumping`：跳跃互动。
- `look`：根据指针位置使用 16 个方向的注视动作。

## 常见问题

### 安装后看不到 Lumina

确认两个文件直接位于 `%USERPROFILE%\.codex\pets\lumina`，不要多套一层文件夹。然后在“设置 → 宠物”中刷新并重新选择 Lumina；仍未更新时，重启桌面应用。

### 动画只有静止画面

宠物会遵循操作系统的“减少动态效果”设置。关闭该设置后重新打开桌面应用，精灵动画即可恢复。

### 更新后仍显示旧版本

确认已经覆盖 `spritesheet.webp` 和 `pet.json`，再刷新宠物列表并重新选择 Lumina。必要时完全退出桌面应用后重新启动。

### 如何卸载

先在“设置 → 宠物”中切换到其他宠物或默认模式，再删除 `%USERPROFILE%\.codex\pets\lumina` 文件夹。

## 隐私说明

仓库发布文件不包含本机绝对路径、个人邮箱、访问令牌、API 密钥或密码。图片文件不包含 EXIF 个人信息。
